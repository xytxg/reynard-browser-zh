diff --git a/layout/base/nsPresContext.h b/layout/base/nsPresContext.h
index 5cdf0f1aa932..3e729fc2aac2 100644
--- a/layout/base/nsPresContext.h
+++ b/layout/base/nsPresContext.h
@@ -395,10 +395,12 @@ class nsPresContext : public nsISupports,
   nsSize GetSizeForViewportUnits() const { return mSizeForViewportUnits; }
 
   /**
-   * Set the maximum height of the dynamic toolbar in nscoord units.
+   * Set the maximum height of the dynamic toolbar in nscoord units, and the
+   * height it keeps when collapsed.
    */
   MOZ_CAN_RUN_SCRIPT
-  void SetDynamicToolbarMaxHeight(mozilla::ScreenIntCoord aHeight);
+  void SetDynamicToolbarMaxHeight(mozilla::ScreenIntCoord aHeight,
+                                  mozilla::ScreenIntCoord aMinHeight = 0);
 
   /**
    * Returns true if we are using the dynamic toolbar.
@@ -416,6 +418,7 @@ class nsPresContext : public nsISupports,
     return mDynamicToolbarMaxHeight;
   }
 
+  // Returns the part of the dynamic toolbar that can hide, in app units.
   nscoord GetDynamicToolbarMaxHeightInAppUnits() const;
 
   mozilla::ScreenIntCoord GetDynamicToolbarHeight() const {
@@ -435,18 +438,16 @@ class nsPresContext : public nsISupports,
   bool IsKeyboardHiddenOrResizesContentMode() const;
 
   /**
-   * Returns the maximum height of the dynamic toolbar if the toolbar state is
-   * `DynamicToolbarState::Collapsed`, otherwise returns zero.
+   * Returns the part of the dynamic toolbar that can hide if the toolbar state
+   * is `DynamicToolbarState::Collapsed`, otherwise returns zero.
    */
   nscoord GetBimodalDynamicToolbarHeightInAppUnits() const;
 
   /**
-   * Returns the maximum height of the dynamic toolbar if the toolbar state is
-   * `DynamicToolbarState::Collapsed`, otherwise returns zero.
-   * The maximum height is based on the fixed viewport scale for elements with
-   * position:fixed.
+   * Returns the hidden part of the dynamic toolbar's height, using the fixed
+   * viewport scale for elements with position:fixed.
    */
-  nscoord GetBimodalDynamicToolbarHeightForFixedPosInAppUnits() const;
+  nscoord GetHiddenDynamicToolbarHeightForFixedPosInAppUnits() const;
 
   /**
    * Returns the state of the dynamic toolbar.
@@ -1256,6 +1257,8 @@ class nsPresContext : public nsISupports,
   nsSize mSizeForViewportUnits;
   // The maximum height of the dynamic toolbar on mobile.
   mozilla::ScreenIntCoord mDynamicToolbarMaxHeight;
+  // The height the dynamic toolbar keeps when collapsed.
+  mozilla::ScreenIntCoord mDynamicToolbarMinHeight;
   mozilla::ScreenIntCoord mDynamicToolbarHeight;
   // Safe area insets support
   mozilla::LayoutDeviceIntMargin mSafeAreaInsets;
